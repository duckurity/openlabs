import express from 'express';const app=express();app.get('/health',(q,s)=>s.json({status:'healthy',service:'integration-router'}));app.get('/status',(q,s)=>s.json({environment:'staging',upstream:'connector-workers'}));app.get('/info',(q,s)=>s.json({service:'Nexora Internal API',build:'2026.09.10'}));app.get('/version',(q,s)=>s.type('text').send('2.14.7'));app.get('/metrics',(q,s)=>s.type('text').send('nexora_requests_total 1842\nnexora_queue_depth 3\n'));app.listen(80,'0.0.0.0');
