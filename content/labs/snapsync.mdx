---
title: Snapsync
description: A staff photo tool where a forged token and a hidden internal route reach the flag.
track: web
difficulty: medium
port: "3000"
type: lab
key: lab-snapsync
slug: labs/snapsync
intent: Solve a medium web lab in Docker.
keywords:
  primary: Snapsync web security lab docker
  secondary:
    - medium web practice
    - openlabs snapsync
verified: true
score: 81
score_grade: B
linksTo:
  - technique/ssrf
author_name: "Mohamed Khaled"
author_url: "https://github.com/duckurity/openlabs/commits/main/labs/web/snapsync"
author_date: "2026-09-10"
ogImage: /og/labs-snapsync.png
---

{/* Generated from labs/web/snapsync/lab.yml and README.md. Do not edit by hand. Run scripts/sync_site_content.py. */}

`medium` · `web`

## Brief

SnapSync is a small internal tool staff use to upload and share work
photos. Every employee gets an account, can upload photos, and can pull
back the ones they own. One route in the app sits outside the normal
employee reach.

## Setup

Run this from the lab directory:

```bash
docker compose up -d --build
```

Open `http://localhost:3000`. Give it a few seconds after startup, then
confirm it is ready:

```bash
curl http://localhost:3000/api/health
```

To return to a clean state:

```bash
./reset.sh
```

## Goal

Starting from a normal account, reach the restricted route and extract
its flag. Verify the solve from the lab directory:

```bash
python3 ../../../scripts/check.py .
```
## Techniques

Practise in this lab:

- [Server-side request forgery (SSRF)](/technique/ssrf)

