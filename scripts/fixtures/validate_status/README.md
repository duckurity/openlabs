# Lab status validator fixtures

Minimal `lab.yml` snippets for `scripts/test_validate_status.py`.

Each subdirectory is one case. The test runner parses `lab.yml` and checks
`check_lab_status()` from `scripts/validate.py`.

Do not treat these directories as playable labs.
